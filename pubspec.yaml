name: networking_app
description: A new Flutter project to demonstrate HTTP and Chopper usage.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6
  chopper: ^5.0.0
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.3.3
  chopper_generator: ^5.0.0



flutter:
  uses-material-design: true
